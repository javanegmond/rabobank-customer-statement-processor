Unzip the zip file. 

Make sure to have java 11 and Maven installed and that port 8080 is available. Open a terminal in the folder where the pom.xml is.

To build and run the tests, run `mvn verify`.

To start the application to try it out, run `mvn spring-boot:run`.

